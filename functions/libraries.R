# libraries used for functions....

library(pryr);              # mem_used()
library(lmtest);            # lmtest has lrtest
library(MASS);              # polr

library(tractor.base);      # implode

library(stringr);           #
library(rvest);             # imdb web scraping

library(tidyverse)         # file cleaning, imdb

require(humanVerseWSU);   # Monte

require(pvclust); # Hclust
require(psych);require(scatterplot3d);require(rgl); # EFA
require(REdaS); require(nFactors);# EFA
require(parallel);require(mvnfast); # PCA
require(scatterplot3d); require(rgl); # PCA 3-d plot